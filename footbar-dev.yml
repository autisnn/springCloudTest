profile: profile-dev
#服务端口号
# server:
#   port: 8053

server:
 port: 8080

spring:
 datasource:
   name: test
   url: jdbc:mysql://127.0.0.1:3306/mybatiestest
   username: root
   password: root
   driver-class-name: com.mysql.jdbc.Driver
mybatis:
 mapper-locations: classpath:*Mapper.xml
 type-aliases-package: ccom.example.bean